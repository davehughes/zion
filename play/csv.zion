module _
link module posix

def main()
	var bufsize __int__ = 1024r
	static_print(bufsize)
	filename := "test.csv"
	buf := posix.calloc(bufsize, (1).raw)
	filedes := posix.open(filename.raw, (0).raw)
	res := posix.read(filedes, buf, bufsize - (1).raw)
	print(buf as __str__)
